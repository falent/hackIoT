export declare class VirtualDevice {
    token: string;
    locale: string | undefined;
    voiceID: string | undefined;
    baseURL: string;
    constructor(token: string, locale?: string | undefined, voiceID?: string | undefined);
    normalizeMessage(message: string): string;
    message(message: string, debug?: boolean): Promise<IVirtualDeviceResult>;
    batchMessage(messages: string[], debug?: boolean): Promise<IVirtualDeviceResult[]>;
    resetSession(locale?: string): Promise<IVirtualDeviceResult>;
    private handleBatchResponse(data);
    private normalizeTranscript(transcript);
}
export interface IVirtualDeviceResult {
    card: ICard | null;
    debug?: {
        rawJSON: any;
    };
    sessionTimeout: number;
    streamURL: string | null;
    transcript: string | null;
    message: string;
}
export interface ICard {
    imageURL: string | null;
    mainTitle: string | null;
    subTitle: string | null;
    textField: string | null;
    type: string;
}
